% Change the path below to the path containing /hw1, then run this script to add the necessary paths to your Matlab search path.

codedir = '/hw1';

%%%% Add all dirs in /hw1 to path
addpath(genpath(codedir));

%%%% Alternatively, just add these necessary paths:
% addpath([codedir filesep 'utils']);
% addpath([codedir filesep 'scripts']);
% addpath([codedir filesep 'plots']); *)
